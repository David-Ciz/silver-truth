# Light augmentation sweep: combine vertical flips and brightness/contrast jitter.
extends: ensemble_sweep_base.yaml

ensemble_model_type: UnetPlusPlus
ensemble_version: C1
ensemble_augmentation: basic_vflip_brightness
